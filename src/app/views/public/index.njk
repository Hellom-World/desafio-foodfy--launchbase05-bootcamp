{% extends "layout.njk" %}

{% block content %}

    <main class="apresentation">
        <div class="desc">
            <h1> As melhores receitas </h1>
            <p>Aprenda a construir os melhores pratos
            com receitas criadas por profissionais
            do mundo inteiro.</p>
        </div>
        <img src="/assets/chef.png" alt="">
    </main>

    <section class="content">
        <h1>Mais Acessadas</h1>
        <div class="cards-all">
                
            {% for recipe in recipes %}
                <div class="card" id='{{recipe.index}}'>
                    <img src="{{recipe.image2}}" alt="{{recipe.title}}">
                    <h4 class="content">{{recipe.title}}</h4>
                    <p class="info">por {{recipe.author}}</p>               
                </div>
            {% endfor %}

        </div>
    </section>

{% endblock %}